{
    "title": "Naukri Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "curlCommand": {
            "title": "cURL Command",
            "type": "string",
            "description": "Paste the cURL command from Chrome DevTools (Network Tab -> Right Click -> Copy as cURL).",
            "editor": "textarea",
            "prefill": "curl 'https://resdex.naukri.com/...' ..."
        },
        "maxResults": {
            "title": "Max Results",
            "type": "integer",
            "description": "Maximum number of profiles to scrape (default: 50).",
            "default": 50
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Select proxies to be used by your crawler.",
            "prefill": {
                "useApifyProxy": true
            },
            "editor": "proxy"
        }
    },
    "required": [
        "curlCommand"
    ]
}